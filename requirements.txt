# DailyPy - 核心依赖
# pip install -r requirements.txt

# 图像处理
Pillow>=9.0
numpy>=1.21

# 数据库
mysql-connector-python>=8.0

# AWS S3
boto3>=1.26

# 视频处理（可选，已有 try/except 降级）
# 安装后可用 moviepy 后端截帧、获取时长；不装则依赖 ffmpeg
moviepy>=1.0

# HTTP 下载
requests>=2.28

# 以下为可选依赖，按需安装：
# opencv-python>=4.5      # cv2: 去水印、视频尺寸/时长的备用后端
# piexif>=1.1.3            # EXIF 写入（set_exif 方法）
